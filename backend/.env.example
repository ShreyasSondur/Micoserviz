# MicroService ERP Backend Environment Variables Example
PROJECT_NAME="MicroService ERP"
API_V1_STR="/api/v1"

# Database Configuration (PostgreSQL connection string or sqlite:///./microservice.db)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Admin Initial Credentials
ADMIN_USERNAME="admin"
ADMIN_EMAIL="admin@microservice.io"
ADMIN_PASSWORD="ChangeMeInProduction123!"

# JWT Configuration
JWT_SECRET="your-secure-jwt-secret-key-at-least-32-characters-long"
JWT_ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Storage Path
STORAGE_ROOT="./storage/MicroServiceData"

# SMTP / Email Configuration (Optional)
DEV_OTP_MODE=True
OTP_EXPIRE_MINUTES=10
SMTP_ENABLED=False
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_TLS=True
SMTP_SSL=False
SMTP_USER=""
SMTP_PASSWORD=""
EMAILS_FROM_EMAIL="noreply@microservice.io"
EMAILS_FROM_NAME="MicroService ERP"
EMAILS_TO_EMAIL=""

# CORS Configuration (Comma-separated allowed origins)
BACKEND_CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"
